name: CI Pipeline

on:
  workflow_dispatch:
  push:
    branches: [ master, day5-review ]
  pull_request:
    branches: [ master, day5-review ]
    
jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run linting (flake8)
        run: |
          pip install flake8
          flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics

      - name: Run tests
        run: |
          pip install pytest
          pytest --maxfail=5 --disable-warnings || echo "No tests found or tests failed - continuing pipeline"